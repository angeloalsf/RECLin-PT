\chapter[Experimentos e Resultados]{Experimentos e Resultados}
\label{cap:experimentos}

Este capítulo reporta os experimentos conduzidos e seus resultados. Os dois
\emph{encoders} descritos no Capítulo~\ref{cap:prototipo} foram ajustados sobre a
mesma partição congelada e avaliados sobre o mesmo conjunto de teste, composto por
$16\,074$ pares candidatos ($14\,959$ \texttt{no\_relation}, $964$
\texttt{associated\_with} e $151$ \texttt{negation\_of}), gerados conforme o
Capítulo~\ref{cap:metodologia}. As métricas seguem o protocolo ali descrito.

\section{Configuração experimental}

Cada \emph{encoder} foi ajustado por $3$ épocas, com \emph{batch} de $64$, taxa de
aprendizado $2\times10^{-5}$ (AdamW) e \texttt{max\_length} de $128$ \emph{tokens},
sob \emph{cross-entropy} ponderada por \texttt{class\_weight="balanced"}. A época
reportada é a de maior macro-F1 no conjunto de validação; o conjunto de teste foi
avaliado uma única vez, ao final. Os dois modelos foram treinados com a semente de
referência $42$ e, como análise de robustez, novamente com a semente $43$ ---
mantendo, em ambos os casos, todos os demais elementos idênticos
(Quadro~\ref{quad:ambiente}).

O conjunto de teste é fortemente desbalanceado: a classe \texttt{no\_relation}
concentra $93{,}06\%$ dos pares. Esse cenário torna o macro-F1 a métrica mais
informativa, por penalizar modelos que negligenciam as classes minoritárias, e faz
da acurácia uma métrica enganosa isoladamente --- um classificador que respondesse
sempre \texttt{no\_relation} acertaria mais de $93\%$ dos candidatos sem qualquer
utilidade clínica.

\section{Curvas de treino e seleção de modelo}
\label{sec:curvas}

A \autoref{fig:curvas} apresenta a evolução por época dos dois \emph{encoders} nas
duas sementes, e a Tabela~\ref{tab:dev_history} traz os mesmos valores em número.
Três observações são relevantes.

Primeiro, a \textbf{perda de treino cai monotonicamente} nas quatro execuções,
enquanto a perda de validação estabiliza ou volta a subir a partir da segunda
época --- comportamento típico de ajuste fino de \emph{encoders} sobre conjuntos
pequenos. Segundo, e mais importante para o protocolo, o \textbf{macro-F1 de
validação continua subindo} mesmo quando a perda de validação piora: é o caso do
BERTimbau na semente $43$, cuja perda de validação sobe de $0{,}437$ (época 2) para
$0{,}489$ (época 3) enquanto o macro-F1 avança de $0{,}624$ para $0{,}674$. Sob
forte desbalanceamento, perda e macro-F1 não são intercambiáveis como critério de
parada, e selecionar pela perda teria descartado a melhor época em termos da
métrica de interesse --- razão pela qual a seleção de modelo usa explicitamente o
macro-F1 de validação.

Terceiro, as curvas tornam visível a \textbf{diferença de estabilidade} entre os
\emph{encoders}. O BERTimbau percorre trajetórias quase sobrepostas nas duas
sementes na perda de treino; o BioBERTpt, não: na semente $43$ ele parte de um
F1 de \texttt{negation\_of} de $0{,}374$ na primeira época, contra $0{,}679$ na
semente $42$, e só recupera terreno ao longo do treino. Essa dispersão, visível
desde a primeira época, é a mesma que reaparece nas métricas finais
(Seção~\ref{sec:robustez}).

\begin{figure}[htbp]
  \centering
  \caption{Evolução por época dos dois \emph{encoders} nas sementes 42 e 43: perda (à esquerda) e F1 no conjunto de validação (à direita). Marcador preenchido indica a época selecionada.}
  \label{fig:curvas}
  \includegraphics[width=\textwidth]{resultados/curvas_treino.png}\\[4pt]
  {\footnotesize Fonte: elaborado pelo autor (\texttt{scripts/make\_tcc\_curves.py}).}
\end{figure}

\input{tabelas/dev_history}

\section{Resultados no conjunto de teste}
\label{sec:resultados}

A Tabela~\ref{tab:resultados} consolida os resultados dos dois \emph{baselines} na
semente de referência. Os melhores valores por coluna estão em negrito.

\input{tabelas/resultados}

Os dois \emph{baselines} apresentam desempenho praticamente indistinguível. O
BioBERTpt clínico atinge macro-F1 de $0{,}707$ e o BERTimbau geral, $0{,}704$ ---
uma diferença de $0{,}003$. Na métrica-alvo, o F1 de \texttt{negation\_of}, a ordem
inclusive se inverte: o BERTimbau geral ($0{,}734$) supera marginalmente o
BioBERTpt clínico ($0{,}724$). As demais métricas seguem o mesmo padrão de quase
empate: ambos alcançam \emph{recall} alto na negação (BioBERTpt $0{,}894$;
BERTimbau $0{,}887$) com precisão moderada ($\approx 0{,}61$--$0{,}63$), e ambos
têm na classe \texttt{associated\_with} a maior dificuldade
(F1 $\approx 0{,}45$--$0{,}46$), com alta cobertura mas baixa precisão. A
\autoref{fig:f1classe} resume o F1 por classe dos dois \emph{baselines}: as barras
são praticamente coincidentes em todas as classes.

\begin{figure}[htbp]
  \centering
  \caption{F1 por classe no conjunto de teste: BioBERTpt (clínico) \emph{vs.} BERTimbau (geral), semente 42.}
  \label{fig:f1classe}
  \includegraphics[width=0.8\textwidth]{resultados/f1_por_classe.png}\\[4pt]
  {\footnotesize Fonte: elaborado pelo autor (\texttt{scripts/make\_tcc\_artifacts.py}).}
\end{figure}

\section{Matrizes de confusão}
\label{sec:confusao}

As Figuras~\ref{fig:cm_biobertpt} e~\ref{fig:cm_bertimbau} apresentam as matrizes
de confusão dos dois \emph{baselines}, \textbf{normalizadas por linha}, de modo que
cada célula indica a fração das instâncias de uma classe verdadeira atribuída a
cada classe predita --- os valores da diagonal correspondem, assim, ao \emph{recall}
por classe. Essa visão complementa o F1 por classe ao revelar \emph{para onde} vão
os erros de cada modelo.

Os dois padrões de erro são notavelmente semelhantes: diagonal forte na negação e
na classe majoritária, e o erro dominante remanescente na confusão entre
\texttt{associated\_with} e \texttt{no\_relation} --- coerente com a baixa precisão
daquela classe nos dois modelos. A semelhança qualitativa entre as matrizes reforça
a leitura de que os dois \emph{encoders} aprendem essencialmente a mesma função
para esta tarefa: não apenas chegam a números próximos, mas erram nos mesmos
lugares.

\begin{figure}[htbp]
  \centering
  \caption{Matriz de confusão do BioBERTpt (clínico), normalizada por linha.}
  \label{fig:cm_biobertpt}
  \includegraphics[width=0.6\textwidth]{resultados/cm_biobertpt.png}\\[4pt]
  {\footnotesize Fonte: elaborado pelo autor (\texttt{scripts/make\_tcc\_artifacts.py}).}
\end{figure}

\begin{figure}[htbp]
  \centering
  \caption{Matriz de confusão do BERTimbau (geral), normalizada por linha.}
  \label{fig:cm_bertimbau}
  \includegraphics[width=0.6\textwidth]{resultados/cm_bertimbau.png}\\[4pt]
  {\footnotesize Fonte: elaborado pelo autor (\texttt{scripts/make\_tcc\_artifacts.py}).}
\end{figure}

\section{Significância estatística}
\label{sec:significancia}

A proximidade dos valores pontuais sugere empate, mas a conclusão exige um teste
pareado. A Tabela~\ref{tab:significancia} reporta a comparação direta entre os dois
\emph{baselines} no conjunto de teste, na semente de referência.

\input{tabelas/significancia}

No F1 de \texttt{negation\_of}, a diferença observada é de apenas $-0{,}010$ (a
favor do BERTimbau geral). O teste de McNemar não rejeita a hipótese nula de
igualdade ($p=0{,}18$): das $946$ instâncias em que os modelos discordam, $494$
favorecem o BioBERTpt e $452$ o BERTimbau, uma assimetria compatível com o acaso. O
\emph{bootstrap} pareado confirma o resultado: o intervalo de confiança de 95\%
para a diferença é $[-0{,}047;\,+0{,}026]$, que \textbf{inclui o zero}, com
valor-$p$ de $0{,}57$. Em ambos os testes, portanto, \textbf{não há diferença
estatisticamente significativa} entre o \emph{encoder} clínico e o geral
\emph{nesta semente} --- ressalva relevante, pois a análise a seguir mostra que
esse veredito não se repete na semente $43$.

\section{Robustez à semente de treino}
\label{sec:robustez}

Para situar a magnitude da diferença entre os \emph{baselines}, ambos foram
treinados com uma segunda semente ($43$), mantendo todo o restante idêntico. A
Tabela~\ref{tab:robustez} consolida as métricas por semente e a amplitude
observada.

\input{tabelas/robustez_semente}

O contraste entre os dois \emph{encoders} é acentuado. No BioBERTpt, o macro-F1 cai
de $0{,}707$ (semente 42) para $0{,}665$ (semente 43), e o F1 de
\texttt{negation\_of}, de $0{,}724$ para $0{,}653$ --- uma amplitude de $0{,}070$
atribuível \emph{apenas} à inicialização aleatória do treino. Essa variação entre
sementes de um \emph{mesmo} modelo é cerca de \textbf{sete vezes maior} que a
diferença de $0{,}010$ observada entre o BioBERTpt e o BERTimbau na semente $42$.
Em outras palavras, o ruído de treino do \emph{encoder} clínico, isoladamente,
supera a vantagem que se poderia atribuir ao domínio de pré-treino.

O BERTimbau, sob a mesma troca de semente, mostra-se sensivelmente mais estável: o
macro-F1 passa de $0{,}704$ para $0{,}706$ e o F1 de \texttt{negation\_of}, de
$0{,}734$ para $0{,}726$ --- uma amplitude de $0{,}008$, cerca de nove vezes menor
que a do BioBERTpt na mesma métrica.

O ponto metodologicamente mais delicado é que o \emph{veredito estatístico} também
muda. A Tabela~\ref{tab:significancia43} aplica à semente $43$ exatamente o mesmo
protocolo da Seção~\ref{sec:significancia}.

\input{tabelas/significancia_seed43}

Na semente $43$, a ordenação entre os dois \emph{baselines} no F1 de negação se
mantém, agora com margem de $0{,}073$, e o mesmo protocolo acusa uma diferença
\textbf{significativa} em favor do BERTimbau (McNemar $p<10^{-30}$; \emph{bootstrap}
pareado IC95\% $[-0{,}115;\,-0{,}031]$, $p=0{,}0002$) --- ao contrário do que se
observa na semente $42$, em que o mesmo teste não rejeita a hipótese nula.

Esse contraste \textbf{não estabelece a superioridade do \emph{encoder} geral}. O
que ele mostra é que o próprio veredito do teste pareado, sob protocolo e conjunto
de teste idênticos, alterna entre ``sem diferença'' e ``diferença significativa''
em função apenas da inicialização do treino. O que atravessa as duas sementes são
dois fatos: o \textbf{sinal} --- em nenhuma delas o pré-treinamento clínico aparece
à frente --- e a \textbf{menor previsibilidade} do \emph{encoder} clínico entre
reinicializações, que, em um cenário de implantação, pesa a favor do geral. A
leitura estatística, essa sim, depende da semente e não deve ser tomada como
definitiva a partir de uma única execução por modelo.
